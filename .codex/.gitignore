# Keep this list explicit so new Codex state stays visible until classified.

# Credentials and local identity
auth.json
installation_id

# History, sessions, and local databases
history.jsonl
sessions/
memories/
*.sqlite*
thread-writer-locks/

# Caches, logs, and temporary state
.tmp/
tmp/
cache/
editor/
log/
models_cache.json
version.json
.personality_migration
.sandbox_migration

# Downloaded plugins and generated skills
plugins/
skills/
mcp-oauth-locks/file-store.lock
session_index.jsonl
sessions/
shell_snapshots/
